#include "main.h"
/**
* print_square - pronts a square
* @size: size of the square
* Return: No Return
*/
void print_square(int size)
{
	int m = 0;
	int o;

	if (n < 1)
		_putchar(10);
	while (m < n)
	{
		for (o = 0; o < n; o++)
		{
			_putchar('#')
		}
		_putchar(10);
		m++;
	}

