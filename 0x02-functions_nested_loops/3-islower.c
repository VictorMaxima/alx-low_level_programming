#include "main.h"
#include <unistd.h>
/**
* _islower - checks if a char is lower case
* @a: char to be evaluated
* Return: q if true, otherwise 0
*/
int _islower(int c)
{
	if (a >= 97 && a <= 122)
		return (1);
	else
		return (0);
}
